<!-- res/values/styles.xml -->
<resources>
    <!-- Base application theme or existing styles -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <!-- Custom style for multi-line MaterialButton -->
    <style name="MyOutlinedButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:singleLine">false</item>
        <item name="android:maxLines">2</item>
        <item name="android:lines">2</item> <!-- Force two lines -->
        <item name="android:ellipsize">none</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:includeFontPadding">false</item>
        <item name="android:gravity">center</item>
        <item name="android:lineSpacingExtra">2dp</item>
    </style>

</resources>
